[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Allowlist public Supabase client keys so Netlify secrets scan doesn't block deploys.
  SECRETS_SCAN_OMIT_KEYS = "VITE_SUPABASE_URL,VITE_SUPABASE_ANON_KEY"
